-- Constructed with love by ImogenOC. Shared and Used under the MIT Licensing.
-- Should be in your OOC section.

-- Water Walking (Shaman)
{ "546", {
	"!player.buff(546).duration <= 60", -- Waterwalking
	"!player.buff(3714).duration <= 60", -- Path of Frost
	"!player.buff(1706).duration <= 60", -- Levitate
}},

-- Water Walking Time Critical
{ "1706", {
	"!player.buff(546).duration <= 60", -- Waterwalking
	"!player.buff(3714).duration <= 60", -- Path of Frost
	"!player.buff(1706).duration <= 60", -- Levitate
}},

-- Path of Ice (Death Knight)
{ "3714", {
	"!player.buff(546).duration <= 60", -- Waterwalking
	"!player.buff(3714).duration <= 60", -- Path of Frost
	"!player.buff(1706).duration <= 60", -- Levitate
}},

-- Path of Ice (Death Knight) Time Critical
{ "3714", {
	"!player.buff(546).duration <= 60", -- Waterwalking
	"!player.buff(3714).duration <= 60", -- Path of Frost
	"!player.buff(1706).duration <= 60", -- Levitate
}},

-- Levitate (Priest)
{ "1706", {
	"!player.buff(546).duration <= 60", -- Waterwalking
	"!player.buff(3714).duration <= 60", -- Path of Frost
	"!player.buff(1706).duration <= 60", -- Levitate
}},

-- Levitate (Priest)
{ "1706", {
	"!player.buff(546).duration <= 60", -- Waterwalking
	"!player.buff(3714).duration <= 60", -- Path of Frost
	"!player.buff(1706).duration <= 60", -- Levitate
}},